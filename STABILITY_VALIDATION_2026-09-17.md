# 安装与首启稳定性验收

日期：2026-09-17。环境：Windows x64，Electron 41.2.1。此轮为本地修复和验证，未发布新版本；安装包版本保持 0.2.31。

## 改动

- 主界面在 IPC 全部注册后加载；macOS 关闭窗口后重新打开时，IPC 使用当前窗口校验。初始化或页面加载失败时提供可见提示。
- Windows 安装先检测 Git / uv，仅对缺失工具下载并执行对应引导阶段。已有工具时，无需访问远程引导脚本。启动、维护、检测和受管 Python 运行均能发现本机工具目录，并处理 Windows 环境变量大小写。
- 已取消的安装不会继续启动下一条命令。欢迎页与设置页等到后台请求结束才开放重试；处理取消请求失败、安装 IPC 拒绝、重试进度重置以及安装完成后状态刷新失败。
- 欢迎页开放已有的 macOS 原生安装流程。macOS 仍需 Git 和 uv，可发现常见 Homebrew 和用户目录位置。
- 界面缓存只保留经过校验的偏好与输入，不再重复保存任务日志、运行状态、加载状态、通知和完整概览。保留主题、发送快捷键、模型选择与活动会话；会话内容从主进程日志恢复。
- 缓存损坏时回退默认偏好，存储受限或写满时在内存中继续运行。React 渲染异常提供恢复界面及重新加载入口。偏好设置可独立加载和保存，无需先加载扩展概览。
- `package.json` 和锁文件未变更；没有新增运行时依赖，也没有改用系统 Python 安装包。

## 验证

| 项目 | 结果 |
| --- | --- |
| TypeScript | `npm run check` 通过 |
| 完整回归 | 74 个测试文件，538/538 项通过；报告：`release/stability-check/tests.json` |
| 最后补充的设置页故障处理 | 6/6 项专项复测通过，包含 2 项新增测试；报告：`release/stability-check/settings-tests.json` |
| 生产构建 | `npm run build` 通过 |
| Windows NSIS | 使用 `--publish never` 构建，仅输出到本地 `release/stability-check` |

新增回归覆盖：远程引导脚本不可用时复用工具、仅安装缺失工具、取消后不执行下一阶段、重启后工具发现、Windows PATH 覆盖、窗口重建、存储损坏和配额耗尽、界面恢复、偏好迁移、发送快捷键恢复、安装取消与刷新失败。

## 安装包验证范围

使用最终打包目录内的实际 Electron 程序执行 `--smoke-test`，数据写入独立临时目录；阻止外部子进程和网络请求，Hermes 指向未安装路径。检查欢迎页、聊天输入框、导航、preload / IPC、SQLite WASM、更新模块和 Python 资源，并观察 15 秒空闲状态。

最终冒烟通过：2026-09-17 11:27:04–11:27:21（北京时间），exit 0，11 项检查全部通过。欢迎页就绪 1,388 ms，聊天界面就绪 1,779 ms；外部子进程启动 0 次，无页面/preload 错误及 HTTP/WebSocket 请求，结束后无该验证包进程残留。报告：`release/stability-check/smoke-report.json`。这些时长为本机单次测量，不作跨机器启动性能保证。

最终安装包：`release/stability-check/Hermes-Forge-0.2.31-x64.exe`，93,830,837 B（约 89.5 MiB）。SHA-256：`b916c8d15897e991eb5041c3836079d0297c7393ed0be5caec4488416e15c1a2`。

该检查验证客户端在没有可用 Hermes / Python、没有外部网络的条件下能进入界面；不替代干净 Windows 虚拟机中的完整联网安装、真实模型请求或 macOS 真机验收。本轮没有执行这些项目。首次下载 Hermes 仍需要网络，真实对话需要配置可用模型及相应凭据。

安装包及最终冒烟结果见 `release/stability-check/`。该目录为本地构建产物，不纳入 Git。
