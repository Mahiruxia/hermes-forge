# Hermes Forge 0.2.31 验收记录

验收日期：2026-09-15。主要环境：Windows x64、Node.js 22+、Electron 41；源码分支 `codex/hermes-0.21.3-lean`。

## 交付范围

- 保留 Electron，主导航整理为聊天、工作区、技能与记忆、设置；扩展默认关闭，迁移保留已有明确启用的功能。
- 原地升级本机 Hermes：官方 `0.21.3` / `v2026.9.14`，固定提交 `345cd2b057a452236de401d3534b8502a7465e8d`。
- 统一受管 Python、官方锁文件同步、维护锁、任务终态、审批和澄清、会话与配置一致性。
- 本轮不发布 GitHub Release，不生成升级备份或自动 stash。

官方依据：[0.21.3 发布说明](https://github.com/NousResearch/hermes-agent/releases/tag/v2026.9.14)。

## 本机升级

| 项目 | 升级前 | 升级后 |
| --- | --- | --- |
| 实际安装版本 | 0.14.0 | 0.21.3 |
| 来源 | 社区镜像 | `https://github.com/NousResearch/hermes-agent.git` |
| 实际提交 | `9f182bd7b04f73e4508999017e20740272b036a8` | `345cd2b057a452236de401d3534b8502a7465e8d` |
| 工作目录 | `%LOCALAPPDATA%/hermes/hermes-agent` | 同一目录 |
| 主环境 | `venv`，Windows Store Python 3.13.14 | 同一 `venv` 路径，uv 管理的 CPython 3.11.15 |
| 重复环境 | 另有 `.venv` | 当前活动环境仅为 `venv`；闲置 `.venv` 清理被自动审批审核拒绝，保留 52,681,616 B |

旧工作树唯一的受跟踪修改为 `uv.lock`，按授权替换为目标官方锁文件。保留配置、密钥、官方 SessionDB、Profile、任务记录和源码。

升级中实测发现 Windows Store 解释器不能用于可靠重建该虚拟环境；维护器现先验证 uv 管理的基础解释器，再在同一环境路径执行同步。实际依赖使用核心、MCP 与已启用消息/模型所需 extras，不使用 `--all-extras`。Kimi Coding 的 Anthropic 协议映射已纳入依赖选择，实际启用 `anthropic`、`mcp`、`messaging`。

真实 `InstallOrchestrator → NativeInstallStrategy.update()` 重复升级通过，总耗时 25.469 秒。Git fetch 13.568 秒，checkout 0.549 秒，locked sync 0.297 秒，包兼容检查 0.281 秒，CLI 检查 2.467 秒，核心导入 6.789 秒。101→101 个包，无安装或卸载；配置字节、Python 可执行文件、venv 配置、锁文件和 HEAD 均不变。管理标记更新、维护锁释放；未新增备份或 stash。

Forge 启动受管运行时设置官方 `HERMES_DISABLE_LAZY_INSTALLS=1`，清除额外惰性安装目标；真实官方 `_allow_lazy_installs()` 返回 false，避免运行时导入隐式扩张依赖。维护仍通过官方锁文件完成。

## 验证结果

### 已完成的契约和回归

- 改动前：54 个测试文件、426 项测试、类型检查及生产构建通过。
- 真实官方 0.21.3：AIAgent、SessionDB、审批/澄清回调与 Gateway 扩展控制契约通过。隔离探针使用本地模型替身和真实官方代码，外部网络访问为 0；它不替代真实模型测试。
- 真实 SessionDB：无数据库时只读查询不创建数据库；创建、重命名、读取、删除可用；损坏数据库明确报错。
- 配置并发：模型/连接器同时修改同一 Home，未知字段、辅助模型与 fallback 保留；飞书普通文件副本同步更新。
- 会话：清空更换官方映射；导出包含官方真实消息；清理日志/快照不会删除同工作区其他会话数据。
- 交互：官方允许的审批范围、请求 ID 匹配、单次消费、超时、拒绝、取消、批量澄清和回复继续执行。
- 生命周期：维护期间禁止运行/启动，启动预检期间拒绝维护；Gateway 停止与重启竞态、失败健康状态和退避恢复覆盖。
- 真实 Node 父子进程树：使用生产默认取消宽限，约 4.4 秒内父子 PID 均退出；仅产生一个取消终态。

最终类型检查与生产构建通过。代理修复后的 `npx vitest run --maxWorkers=2`：70 个测试文件、517/517 项通过；Python `unittest discover`：14/14 项通过。另复测受管环境政策 4 项全部通过。此前并行高负载的一轮中，旧日志 I/O 测试超时；单独复测及限制并行数后的全量测试均通过，没有放宽测试断言。

### Windows 真实模型与最终安装包

最终 NSIS 构建通过；从该构建的 `win-unpacked/Hermes Forge.exe` 运行以下检查。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 离线实际窗口 / preload / IPC | 通过 | 全新欢迎页和真实聊天输入框、导航均渲染；IPC 返回隔离配置/会话 |
| 主进程运行依赖 | 通过 | electron-updater 加载，实际 SqliteMemoryIndex 执行 SQL 查询，5 个 Python 资源齐全 |
| 五分钟默认空闲 | 通过，exit 0 | 2026-09-15 15:16:02–15:21:03；无 Gateway/探测/setup/overview/扩展数据列表 IPC |
| 空闲外部进程 / 网络 | 通过 | 0 次 child_process spawn/fork/exec，0 次 HTTP/WebSocket 请求，0 个页面/preload 错误 |
| Kimi 真实聊天 | 通过 | 最终包 36.814 秒，真实成功结果 |
| 跨进程官方会话恢复 | 首轮 0.2.31 包通过 | 两个不同 taskRunId 的进程，官方同一 session ID，消息数 2→4；第二次提示不含随机口令 |
| 中文 / 特殊字符路径附件 | 最终包单项复测通过，保留一次超时记录 | 首轮 39.551 秒通过；最终完整审计触发 180 秒超时；随后仅复测一次，97.083 秒成功返回实际附件随机标记，5 次工具调用完成 |
| 约 9 MiB 大文件 | 通过 | 最终包 105.661 秒，成功工具事件与真实文件尾标记同时匹配 |
| 宿主命令 | 通过 | 最终包 80.347 秒，成功 terminal/command 工具事件及命令输出匹配 |
| MiMo 真实切换 | 未通过，现有密钥被拒绝 | 在 Electron 内使用原凭据直接请求实际端点，HTTP 401 Invalid API Key；需要用户在设置更新密钥 |
| 工作区外写入 | 跳过 | 普通审计不执行该深度项目 |

会话恢复项验证 Hermes 执行进程重启；Electron 整个应用关闭重开的点击流程未做自动化验收。

最终包的完整普通模型审计因附件超时返回 exit 1，不能计为整轮通过。随后使用同一最终 app.asar（SHA-256：`9d546213a34d33f2f42b9099854fffdcdc748d6fd2ac54c1846e6f9c054b059b`）和 Python 资源做一次附件定向复测：`result.success=true`、`outcome=completed`，真实随机标记匹配，用时 97.083 秒；未进行循环重试。当前证据不能确定此前超时的唯一原因。所有检查结束后按最终应用和受管 Python 的可执行路径检查进程，均无残留。

MiMo 首轮完整审计按设计返回 exit 1。排查发现两个独立原因：现有凭据被上游拒绝；旧代理的流式请求体使 Node 把上游 401 包装为 502 `fetch failed`。代理已改为可重放 JSON 请求体，保留真实上游错误，限制同源重定向，并处理 fetch 解压后响应头；新增真实 HTTP 401、同源 307、跨源拒绝及 gzip 响应用例。该修复没有修改任何模型选择或密钥。再从最终包的 app.asar 加载真实代理，使用原凭据重复单次请求，已准确返回 HTTP 401 / Invalid API Key，确认误报已修复。

## 体积、启动与空闲实测

| 指标 | 基线 | 最终值与口径 |
| --- | --- | --- |
| 首个 HTML 入口预加载 JS | 635,159 B；gzip 195,122 B | 252,240 B；gzip 79,386 B，入口分别减少 60.3% / 59.3% |
| 安装包 | 最近旧包 0.2.27：109,282,825 B | 93,829,340 B（89.48 MiB），减少 14.1%；旧包版本不同 |
| app.asar | 旧 0.2.27：75,179,303 B | 8,680,457 B（8.28 MiB），减少 88.5% |
| 本地 release 目录 | 约 1,542 MiB | 1,597,154,520 B（1,523.17 MiB）；旧文件清理被拒绝，仍有 12 个安装器及解包目录 |
| Hermes 主 venv | 约 1,203 MiB | 227,344,529 B（216.81 MiB），101 个锁定包 |
| 多余 .venv | 约 50 MiB | 50.24 MiB，已确认闲置；清理被自动审批审核拒绝 |
| 欢迎界面就绪 | 没有同条件旧版数据 | 1,225 ms，从进程启动计时 |
| 聊天界面就绪 | 没有同条件旧版数据 | 1,598 ms，从进程启动计时，包含欢迎页及一次真实重新加载 |
| 五分钟空闲 | 没有同条件旧版内存数据 | 4 个 Electron 进程总工作集 421,056→408,580 KiB（411.2→399.0 MiB），外部进程启动 0 次 |

入口 JS 指初始 HTML 预加载资源，不等同完整聊天界面的全部懒加载资源。最终聊天所需 JS 合计 732,985 B / gzip 225,331 B。不能把入口缩减比例当作整体页面资源或启动时间改善比例。内存包含 Electron 主进程、渲染器、GPU 和 utility 进程，以隐藏窗口空闲实测。

最终 Windows 安装包 `Hermes-Forge-0.2.31-x64.exe`，SHA-256：`88e3f22cb32455c3e291206dca09c5d6b8a6489687521b30a9361e791c28e1b9`。

## 平台及验证边界

- macOS 原生安装/启动分支、路径及受管解释器选择已有测试，CI 已改为真实离线包冒烟。但本轮没有 macOS 主机，因此没有执行 macOS DMG/ZIP 构建或包运行验收。
- 旧 main、旧标签、detached HEAD、断网及中断恢复的覆盖应以安装器测试与本机重复升级记录区分；没有人为断开本机网络或中断实际升级。
- 未向真实消息账号发消息；连接器进程与状态测试不替代平台端收发联调。
- 普通系统审计跳过工作区外写入；这一项不计作通过。

## 清理例外

自动审批审核拒绝清理两个早期冒烟测试的临时目录，返回原因仅为 `blocked by policy`。未换用其他方式重试；保留约 203 KiB：

- `%LOCALAPPDATA%/Temp/hermes-forge-smoke-gh8ga1`
- `%LOCALAPPDATA%/Temp/hermes-forge-smoke-lHY9OV`

最终构建前清理工作区 `dist/main` 也被自动审批审核以同一原因拒绝，未重试删除。只读比对确认唯一已删除源码对应的旧编译模块为 `dist/main/install/hermes-capabilities.js`；打包清单明确排除了它，最终包不携带该旧模块。磁盘上的构建目录保留。

本机 `.venv` 清理在校验绝对父目录、拒绝符号链接、检查运行时配置及进程占用后，仍被自动审批审核以 `blocked by policy` 拒绝，命令未执行，未换用其他方式重试。保留 52,681,616 B（50.24 MiB）；受管运行时和配置均指向 `venv`。

对 release 中 22 个明确列名的旧安装器、旧 blockmap 与旧发布说明文件，使用不递归的逐文件删除并核验直属父目录，仍被自动审批审核以相同原因拒绝。旧发布文件保持原样；“release 最终只保留一个安装包”的清理目标未完成。未改用其他删除方式。

最终测试结束、确认进程退出后，对 `release/win-unpacked` 单独校验绝对父目录、目录名称及符号链接，再尝试清理，也被自动审批审核以 `blocked by policy` 拒绝，命令未执行。解包目录保留，未改用其他删除方式。

目录体积按文件长度求和，不等同文件系统实际释放的物理空间（例如 uv 缓存可能使用硬链接）。升级备份未创建；本轮诊断记录保留在应用 diagnostics 中。
